{
  "name": "@resume-vault/jd-fetch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "fetch": "npm run build --silent && node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "playwright": "^1.51.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
